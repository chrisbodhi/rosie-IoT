'use strict';

/**
 * @ngdoc function
 * @name rosieApp.controller:SettingsCtrl
 * @description
 * # SettingsCtrl
 * Controller of the rosieApp
 */
angular.module('rosieApp')
  .controller('SettingsCtrl', function ($scope) {
    $scope.info = 'Settings go here, nerd!';
  });
